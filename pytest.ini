[pytest]
# Opzioni di esecuzione: -v (verbose) e -q (quiet)
addopts = -v
# Directory dove cercare i test
testpaths = tests
# Configura asyncio per testare endpoint asincroni (richiede pytest-asyncio)
asyncio_mode = auto
# Ignora le directory di build o venv
norecursedirs = venv .venv build dist
